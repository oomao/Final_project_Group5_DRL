# Training artifacts
runs/

# Evaluation reports (figures + markdown produced by tools/compare_conditions.py)
reports/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.ruff_cache/

# Model checkpoints
*.pt
*.pth

# Virtual environments (root-anchored so it does NOT match hermes_dqn/env/)
/.venv/
/venv/
/env/

# Secrets (API keys, etc.) — never commit
.env
.env.local
*.env.local

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# LaTeX build artifacts
paper/latex_build/*.aux
paper/latex_build/*.log
paper/latex_build/*.out
paper/latex_build/*.synctex.gz
paper/latex_build/en_main.pdf
paper/latex_build/zh_main.pdf

# Slide-skill working area (cloned repos + alt decks, not part of the project)
_slide_skills/
PPT/_tmp_preview/

# Final Project deck: reference example + speaker notes are LOCAL-ONLY (not uploaded)
PPT/期末報告簡報/PPT參考.pptx
PPT/期末報告簡報/口白_15分鐘.md
paper/conference_talk_15min_zh.md
