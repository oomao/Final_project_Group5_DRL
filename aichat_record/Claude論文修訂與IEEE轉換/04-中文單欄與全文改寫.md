# 中文單欄與全文改寫

> 接續 03,本段為同一 session 後續的對話紀錄

## 三個小修

- *§4 標題層級*:使用者發現「IV. Experiments / A. Setup / B. Results / C. LunarLander」有問題——LunarLander 等分環境小節跟 Setup/Results 同層(都是 H3)。改成 `#### 4.2.x`(H4),build.py 的 `cleanup_body` 加上 `\paragraph → \subsubsection` 提升,讓它們正確嵌在 Results 底下(渲染成「1) LunarLander…」)。
- *頁碼*:中英版原本沒頁碼(IEEEtran conference 預設不顯示)。兩版 preamble 加 `\pagestyle{plain}` + `\thispagestyle{plain}`。

## 中文版改單欄

- 使用者:「兩欄版面對中文太擠、很難閱讀」。
- 處理:中文版 `\documentclass` 加 `onecolumn`,英文版維持 IEEE 兩欄。
- 之後又反映還是擠 → 進一步調可讀性:行距 1.35、段落間距 (parskip 3pt)、邊界加寬到 1.25 吋(行變短、約 32 字/行)。試過 11pt/1.5 但 21 頁太腫,最後落在 **10pt / 1.35 / 單欄 = 16 頁**。

## 中文全文改寫(anti-AIGC 人味)

使用者要求中文「不只是翻譯」,要照中文改寫、避開知網 AIGC 偵測。具體規則:多用「的了到過會有能把」等粒子、用「一是/二是/三是」取代「首先/其次/最後」、少用句號改逗號分號、換同義詞、調語序、**不用「我們/大家」**,但不要大白話也不要太專業。

- 第一版改寫(白話、好懂)被打回「不太像」→ 第二版照上面規則改。
- §1–§7 加附錄全部重寫一遍,所有數字、`[N]` 引用、表格/圖參照、技術術語原封不動。
- 驗證:全文「我們/大家/首先/其次」殘留 = 0。
- 詳細寫作風格已存進記憶(`feedback_zh_writing_style.md`)。

## 收尾

英文版 IEEE 兩欄、中文版單欄好讀,兩版頁碼齊全,一起 commit + push。
